{
  "hooks": [
    {
      "event": "file-changed",
      "match": ["**/models/**/*.ts", "**/models/**/*.js", "**/schemas/**/*.ts", "**/schemas/**/*.js"],
      "actions": [
        {
          "type": "notify",
          "message": "MongoDB model changed. Ensure indexes are defined on the schema and match your query patterns. Run 'db.collection.getIndexes()' to verify.",
          "severity": "info"
        }
      ]
    },
    {
      "event": "file-changed",
      "match": ["**/migrations/**/*.ts", "**/migrations/**/*.js"],
      "actions": [
        {
          "type": "notify",
          "message": "MongoDB migration file changed. Review the migration carefully before running it against your database.",
          "severity": "warning"
        }
      ]
    },
    {
      "event": "file-changed",
      "match": ["docker-compose.yml", "docker-compose.yaml"],
      "actions": [
        {
          "type": "notify",
          "message": "Docker Compose file changed. If MongoDB service was modified, restart with 'docker compose up -d mongodb'.",
          "severity": "info"
        }
      ]
    }
  ]
}
