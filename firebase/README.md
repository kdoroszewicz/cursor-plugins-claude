# Firebase Plugin

Cursor plugin for Firebase — Firestore, Authentication, Cloud Functions, Hosting, and Storage.

## Installation

```bash
agent install firebase
```

## Components

### Skills

| Skill | Description |
|:------|:------------|
| `setup-firebase` | Project initialization, SDK setup, emulator configuration, and deployment |
| `design-firestore-schema` | Data modeling patterns, subcollections, denormalization, and security rules |

### MCP Server

Provides Firebase management via `firebase-mcp-server`.

Requires `FIREBASE_TOKEN` environment variable.

## License

MIT
